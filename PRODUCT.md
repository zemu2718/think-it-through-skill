# 想清楚 · Think It Through 产品设计

> 不替你决定，帮你在决定前想清楚，并让现实结果能够修正判断。

本文档说明 **v0.2.0** 的产品愿景、目标用户、用户价值与核心体验，不构成逐项实现或验收规范。正式行为、安全与验收合同以 [REQUIREMENTS.md](./REQUIREMENTS.md) 为唯一依据。

## 1. 产品定位

AI 越擅长执行，越容易让人忽略一个更早的问题：**这个动作真的服务于我想要的结果吗？**

用户说“写一份推广方案”，真正需要的可能是验证客户是否愿意付费；用户说“把产品做出来”，真正缺少的可能是确认需求是否存在。AI 如果立即执行，会高质量地完成错误动作，并让错误方向变得更昂贵、更难放弃。

“想清楚”是重要行动前后的**决策与证据协议**。它先把表面任务收敛为真实目的和当前决定，再找出最可能改变决定的一个答案。用户回答后，只有外部事实或独立参与真的会改变判断时，才按授权升级到有界调研、额外 Agent 或真人参与；最终始终交付一个综合判断、一个主现实证据闭环、可复制的决策快照和复判入口。

一句话定位：

> **在你让 AI、自己或团队投入重要资源之前，帮你确认真正要解决什么、还缺哪类关键证据，并给出可执行、可复判的解决方案。**

品牌表达：

> **别让 AI 把错误的任务完成得无比漂亮。**

## 2. 首要用户与市场边界

### 2.1 首要 ICP

首要用户是资源有限、行动自主权高、却没有完整研究与反方审查团队的人：

- AI 原生的一人公司和小微企业经营者；
- 独立开发者和小团队负责人；
- 同时承担产品、技术、营销、销售、交付或经营判断的人。

他们通常不缺执行力，而是每个重要选择都会挤占其他机会。一个方向做错，代价不仅是眼前投入，也包括被推迟的替代路径。

产品能力仍适用于职业、团队、合作、关系、高成本选择和已执行事项，但早期市场传播不把所有场景并列为同等主叙事。**能力可以通用，首要定位必须聚焦。**

### 2.2 首要 JTBD

当我准备让 AI、自己或团队对一个重要方向投入时间、金钱、声誉或关系成本时，帮助我：

1. 确认眼前动作是否服务真实目的；
2. 看清现在真正需要决定什么；
3. 分清哪个未知应问我、哪个事实应由系统查、哪个问题只能由真人或现实行为回答；
4. 获得一个不是观点拼盘的综合判断；
5. 只承诺一个能产生新证据、保留回头空间的下一步；
6. 在结果出现后判断继续、调整、暂停还是停止。

### 2.3 用户采用的不是“更多方法”

用户真正需要的是：

- 减少错误方向上的高质量执行；
- 减少不必要的全面投入；
- 更快定位真正会改变决定的证据；
- 在信息不完整时仍形成可行动判断；
- 降低被 AI 的流畅表达、推荐答案或虚假确定性带偏的概率；
- 保留对价值、风险、数据和外部行动的最终控制权。

因此，功能是否进入产品只看它能否改善决定或复判，不看它是否增加框架、角色、报告、Agent 或调用量。

## 3. 用户的核心困境

### 3.1 问题还没定义清楚，答案已经开始生成

用户表面上在问“要不要做 A”，真正需要解决的可能是“怎样验证 B”。问题定义错了，后续越高效，偏离真实目的越远。

### 3.2 已有明显倾向，只是在寻找支持

AI 如果顺着叙述给建议，会放大确认偏误；如果为了显得客观而机械反对，又只会增加噪声。用户需要的是当前方向与最强替代方向接受相近证据标准的检验。

### 3.3 正反意见都有道理，却不知道分歧在哪里

分歧可能来自事实、预测、目标、约束或风险偏好。更多观点不能代替识别真正会改变选择的变量。

### 3.4 该查的事实没有查，不该查的价值被搜索代答

市场价格、法规和平台政策可能直接反转判断；用户底线、团队承诺和客户真实付费意愿却不能由搜索或模型代答。错误的证据来源会制造更可信的错觉。

### 3.5 多 Agent 看起来更专业，却没有独立增量

多个 Agent 复述同一框架、共享同一来源或按多数票决定，不会提高证据质量，反而增加成本、延迟和权威幻觉。

### 3.6 信息永远不完整，因此无法行动

重要选择很少能等到完整信息。真正需要的不是无限补充背景，而是识别哪个未知最可能改变决定，并把其他内容保留为假设。

### 3.7 一次分析结束后，无法用结果校准

如果没有保存当时的目的、事实、假设、反转信号和实验边界，用户容易用结果反写理由、因沉没成本继续，或把执行偏差误判为方向错误。

## 4. 产品如何工作：决定、证据、参与与宿主

v0.2.0 不把所有能力塞进方法卡，而采用四层模型：

```text
1. Decision Core（决定层）
   真实目的、当前决定、竞争判断、关键变量、判断与反转条件

2. Evidence Policy（证据层）
   用户事实、公开事实、私有数据、专业意见、现实行为证据

3. Participation Policy（参与层）
   当前主 Agent、额外 Agent、真实利益相关者、专业人士

4. Host & Action Adapter（宿主与行动层）
   纯文本、Claude Code、ChatGPT；控件、搜索、文件、发送与持久化
```

### 4.1 决定核心（Decision Core）

决定层保持宿主无关，负责：

- 表面任务 → 真实目的 → 决策问题；
- 可并存目的合并与真实排他约束识别；
- 最小方法路由、透明推荐和最终组合确认；
- 事实、推断、假设、未知及分歧类型；
- 一个决策敏感答案；
- 一个综合判断、成立条件和反转条件；
- 一个围绕核心假设的主现实证据闭环；
- 完成本轮或重新进入对齐的条件。

它不依赖某个模型、专有控件、固定 Markdown、联网、固定 Agent 数量或持久化服务。

### 4.2 证据策略（Evidence Policy）

证据层判断“答案应从哪里来”：

| 关键未知 | 默认来源 | 产品动作 |
| --- | --- | --- |
| 目的、底线、风险承受 | 用户本人 | 只问一个决定敏感问题，不让 Agent 代答 |
| 权责、承诺、利益冲突 | 对应真人 | 生成最小参与请求，未经授权不发送 |
| 公开、可核实且具有时效性的事实 | 有界调研 | 获取来源、日期、反证、冲突与限制 |
| 私有业务事实 | 用户指定的数据源 | 最小范围读取，单独取得私有数据授权 |
| 专业高风险判断 | 合格专业人士 | 整理咨询重点，不由 Agent 投票替代 |
| 只能通过真实反应获知 | 现实实验 | 用行为证据复判 |
| 无法可靠预测的深度不确定性 | 稳健性设计 | 降低承诺、分阶段复判 |

默认零外部调用。调研必须服务一个会改变决定的事实问题，并具有范围和停止条件。

### 4.3 参与策略（Participation Policy）

参与层决定谁参与、为什么参与：

- 默认只使用当前主 Agent；
- 只有不重复、可验证、可停止的独立任务才建议额外 Agent；
- 真人掌握的价值、权责、承诺、付款意愿和专业责任必须交给对应真人；
- 主 Agent 始终负责来源去重、冲突呈现和最终综合；
- Agent 结果是材料，不是投票权威。

### 4.4 宿主与行动适配（Host & Action Adapter）

适配层把核心意图映射到当前会话真实具备的能力：

- 单选、多选、自由回答或文本降级；
- 公开搜索、私有连接器、文件读写；
- 子 Agent、并行编排和真人请求；
- 会话状态、决策快照和持久化；
- 发送、发布、购买、删除或修改；
- 权限、失败与执行回执。

宿主能力只改变交互表面和可执行范围，不改变目的对齐、用户确认、授权分离、等待边界和证据诚实。

## 5. 核心体验

用户感受到的不是状态机，而是一段渐进、克制的协作：

```text
先接住我
→ 帮我说清真正要解决什么
→ 在同一选择单元看懂并确认思考角度
→ 只问一个会改变决定的问题
→ 必要时说明为什么值得调研或增加参与
→ 给我一个综合判断和一个现实证据闭环
→ 留下快照，让结果回来后能够复判
```

内部状态为：

```text
空状态 → R-align → R-method → A
→（必要且获授权时）Evidence Gate / Participation Gate
→ B → 反馈 / 结束 / 新一轮 R
```

Gate 是条件能力，不是固定第四阶段。能力不必要、不可用、未授权、被拒绝或失败时，系统基于已有信息进入条件化 B，不让用户停在等待状态。

### 5.1 R-align：先说清真实目的

产品不因为看到一个想法就猜它是为了赚钱、增长或创业。目的未清时，只处理当前最大的一个目的未知：

- 可并存方向使用多选；
- 真实有限互斥边界使用单选；
- 开放答案直接自由回答，不预造选项；
- 原生控件可用时必须实际调用，Markdown 不能冒充工具；
- 控件不可用、失败或被拒绝时立即给语义等价的文本路径；
- 产品不创建“其他/Other”；宿主 `Other` 是同等自由输入；
- 选择与文字冲突时以文字为准。

多个目的可以并存时，先用用户语言合并，不默认排序。只有用户给出的真实排他约束会改变当前决定时，才再澄清一次。

### 5.2 R-method：单屏内联推荐与确认

目的与决定清楚后，系统在后台按“基本分析是否足够 → 双向钢人 → 失败预演 → 至多一张填补独特缺口的专项卡 → 去重”路由，只呈现 0～3 个有增量的方法。

每个候选在同一连续理解与选择单元中包含：

```text
正式方法名
+ 推荐状态
+ 当前议题中的具体价值
+ 选择入口
```

例如：

```text
1. 对象校准（推荐）
   分清使用者、付费者和承担代价的人，帮助判断应该先验证谁的需求。

2. 双向钢人（推荐）
   用相近证据标准检验当前方向和最强替代方向，避免只证明已有倾向。
```

“推荐”不等于确认。只有用户本轮提交的集合或明确文字形成唯一最终组合。用户可以加入、取消、替换或纠正；未确认或已取消的方法不得暗中执行。非推荐项也必须相关且有独特增量，不为凑数量加入无关方法。

“单屏”表示同一连续理解与选择单元，不是硬塞进物理屏幕，也不按字符宽度硬折行。

### 5.3 A：只回答一个真正关键的问题

系统综合已确认方法，不让角色轮流发言，也不逐卡展示框架。它最终只索取一个答案：

> 哪一个答案一旦不同，最可能改变方案排序、行动方向或是否继续？

答案有限且互斥时使用单选；答案开放时直接自由回答。无论哪种形式，都只有一个答案槽。问题提出后立即等待，不替用户回答，不提前给判断、动作、调研或协作建议。

A 可以复用用户已经给出的数字，但不自行植入样本量、期限、金额、人数、比例、成功阈值或未来投入长度。

### 5.4 Gate：只在决定质量值得时升级

用户回答 A 后，系统才路由关键未知：

- 用户价值、底线或风险承受 → 保留用户答案或未知；
- 公开、时效性强且会反转判断的事实 → 评估 Evidence Gate；
- 不重复、可验证、可停止的独立任务 → 评估 Participation Gate；
- 权责、承诺、客户行为或专业责任 → 生成真人参与材料；
- 只能由现实反应回答 → 留给主现实实验；
- 其他 → 直接进入 B。

#### Evidence Gate

只有外部事实会改变方案排序、是否继续或投入边界，并且获取方式明确、有限、可停止、价值高于成本时才建议调研。启用前说明：

- 要改变的决定与关键证据问题；
- 市场、地区、时间和对象范围；
- 来源要求与停止条件；
- provider、数据边界、相对成本和延迟；
- 失败后怎样继续。

初始正式能力只承诺公开、只读、有来源的有界调研。结果同时保留支持证据、反对证据、冲突、空白、日期和对判断的影响。失败时保留未知、降低承诺并进入 B。

#### Participation Gate

默认单 Agent。只有明确独立增量时才建议额外 Agent。用户控制总上限和边界，不需要管理角色拓扑：

```text
可用额外上限 = max(0, 用户总参与上限 - 1)

实际额外数 = min(
  独立增量任务数，
  可用额外上限，
  产品额外 Agent 安全上限，
  宿主可用额外 Agent 上限，
  成本与延迟预算允许数量
)

实际总参与数 = 1 + 实际额外数
```

启用前在一个连续选择单元中说明具体任务、主/额外/总数、数据范围、相对成本和延迟、失败降级，并允许按建议启用、降低数量或保持单 Agent。额外 Agent 不得递归委派；主 Agent 去重来源、呈现冲突并综合，不按多数票决定。

真人掌握的价值、权责、承诺、客户真实意愿和专业责任不能由更多 Agent 替代。默认只生成可转发材料；发送、邀请或联系需要独立外部行动授权。

### 5.5 B：解决方案、主证据闭环与快照

无论 Gate 未进入、被拒绝、失败还是完成，最终都进入 B。用户获得：

1. 一个明确但可调整的判断；
2. 推荐方向、关键依据、成立条件和反转条件；
3. 一个围绕同一核心假设的主现实证据闭环；
4. 一份可复制的决策快照；
5. 四项反馈入口。

判断状态：

| 尚未执行 | 已经执行 |
| --- | --- |
| 暂不行动 / 小步验证 / 有条件推进 / 可以推进 | 继续 / 调整 / 暂停 / 停止 |

主现实证据闭环由动作、观察和复判组成。它可以包含必要的连续操作或多人参与，但所有动作必须共享同一核心假设、资源边界、观察标准和复判点；多个无关项目不能被包装成一个实验。

默认决策快照只在对话中呈现：

```markdown
## 决策快照

议题：
真正目的：
本轮决定：
当前判断：
推荐方向及理由：
已确认事实：
关键推断：
关键假设与未知：
证据来源：
会改变判断的信号：
主现实实验：
复判触发：
参与与能力记录：
```

默认不写文件、不上传、不保存长期偏好，也不保存隐藏思维链。用户之后粘贴快照或授权读取时，系统区分判断错误、执行偏差、资源错配和条件变化，再更新继续、调整、暂停或停止判断。

完整内容后，B 收集一个主要反馈方向：

- 方向符合我；
- 调整下一步；
- 不同意这个判断；
- 暂时先放一放。

原生单选可用时必须实际调用。只有实际观察到独立附注时才说明可并存备注；否则选中后通过普通消息补充。反馈不执行实验，也不授权能力、委派、私有数据、持久化或外部行动。

## 6. 四类授权与能力诚实

v0.2.0 将授权分为四类，彼此不继承：

1. **能力调用**：调用搜索、读取、写入或其他工具；
2. **参与 / 委派**：增加 Agent 或请求真人参与；
3. **私有数据访问**：读取具体私有资源；
4. **外部行动**：发送、发布、购买、删除或修改外部世界。

必须保持：

```text
允许多 Agent
≠ 允许公开联网
≠ 允许读取私有数据
≠ 允许发送、发布或修改外部世界
```

每项能力记录 `available / unavailable / unknown` 与 `ready / requires_approval / requires_auth / failed`。`unknown` 不得当作可用。真实调用产生 provider、范围、来源、实际状态、权限、失败和降级回执；未调用不得生成虚假“完成”记录。

## 7. 跨宿主体验

### 7.1 纯文本是一等参考实现

没有原生控件、搜索、子 Agent 或持久化时，纯文本仍保留完整状态、方法透明、授权边界、主现实闭环、决策快照和 B 反馈路由。文本降级使用普通编号，不使用伪 checkbox、radio 或 HTML 冒充控件。

### 7.2 Claude Code

v0.2.0 的正式入口是在 Claude Code 中显式调用 `/think-it-through`。适配层逐会话映射实际可用的原生选择、搜索、文件、Agent 和权限工具；自然语言自动发现不作为当前发布入口。

### 7.3 ChatGPT 与其他宿主

v0.2.0 为 ChatGPT 与其他宿主定义 Skill-only / 纯文本语义映射，以纯文本路径保持核心状态、方法确认、授权和决策快照。Adapter 不构成原生兼容认证，也不推定通用原生控件、搜索、MCP、子 Agent 或持久化；宿主额外能力只在当前会话实际暴露并按对应授权与回执合同执行时启用。

## 8. 信息呈现原则

表达应像有经验、值得信赖的思考搭档，而不是咨询报告或虚拟专家会议：

- 一意一段；承接、解释、操作提示和正式问题用空行分开；
- 段落用于完整观点；
- 有序列表只用于步骤、依赖、优先级或编号回复；
- 无序列表用于平行事实、理由、风险和限制；
- 标签—值用于授权范围、协作计划和回执；
- 表格只用于同维度横向比较；
- 标题只用于多个需要独立扫描的内容块；
- 不按固定字符数硬折行，不为了版式制造报告腔；
- 正式方法名可辨识，但后台状态、路由分数和内部 ID 不成为用户负担；
- 不自称朋友、导师或教练，不模仿人物口吻。

## 9. 产品原则

1. **目的优先于动作。** 眼前动作不服务真实目的时，先指出错位。
2. **解决方案优先，但不越过用户价值权。** 系统承担综合判断，用户保留价值、底线和最终选择。
3. **最小必要分析优先于全面展示。** 更多框架、角色和篇幅不是专业证明。
4. **单 Agent 默认，但不是产品上限。** 实际数量由独立任务和用户上限共同决定。
5. **该调研时有界调研，不该调研时零调用最好。**
6. **可查事实由系统找，价值、承诺和责任由真实的人决定。**
7. **证据而非投票决定置信度。** 相同来源只算一次，模型一致性不是现实事实。
8. **一个关键答案和一个主证据闭环保持产品克制。**
9. **可验证、尽量可撤回优先于全面投入。**
10. **默认对话内、默认不持久化、默认不改变外部世界。**
11. **每个判断都留下反转条件和复判触发。**
12. **暂停、停止和转向与推进同样是有效结果。**
13. **不以方法、角色、Agent、调用或报告数量证明价值。**

## 10. 产品边界与非目标

“想清楚”不是：

- 替用户作出价值选择的 AI 决策器；
- 默认联网、无上限搜索的研究平台；
- 固定 AI 高管或常驻专家团；
- 多 Agent 投票系统；
- 观点、人物、流派或方法百科；
- 让用户先配置 Agent 拓扑和复杂工作流的编排器；
- 默认读取所有私有数据或保存人格画像的长期记忆系统；
- 未经授权自动联系客户、发送问卷、发布、购买、删除或修改的执行器；
- 医疗、心理、法律、投资等持证专业意见的替代品；
- 对第三方内心、动机或未来行为作确定性推断的工具。

对于事实查询、清晰低风险执行、纯创作和娱乐，最好的体验通常是直接回答或完成。对于紧急安全事件，应优先保护人身安全，而不是延长决策流程。

## 11. 成功定义

产品成功不以调用量、Agent 数或推进率衡量，而看：

- 用户能否准确表达真实目的和当前决定；
- 方法推荐是否容易理解、确认和纠正；
- 唯一问题是否真正改变排序、承诺或实验设计；
- 调研是否实际改变判断，而不是增加篇幅；
- 额外 Agent 是否发现单 Agent 未发现的关键反证或来源冲突；
- 用户是否理解判断依据、反转条件、实际数量和授权边界；
- 现实实验是否降低高成本、低证据的全面投入；
- 决策快照是否支持后续继续、调整、暂停或停止；
- 未授权能力、数据访问和外部行动是否始终为零；
- 用户是否始终清楚最终决定由自己作出。

## 12. 发布范围与声明原则

v0.2.0 是当前正式版本，支持 Claude Code 中显式 `/think-it-through` 入口，并以纯文本协议作为跨宿主基线。原生控件、搜索、额外 Agent、私有数据、持久化和外部行动属于会话条件能力：只有当前会话实际提供、路由条件成立并取得对应授权时才使用。

产品对外声明遵循三条边界：

- Adapter 定义协议映射，不等于对应宿主通过原生兼容认证；
- Schema、fixture、评分器和单元测试维护合同质量，不替代具体会话的能力记录；
- 搜索、Agent、真人参与、持久化和外部行动是否发生，只由当次 capability observation、consent、trace 与 receipt 建立。

新的宿主原生兼容或体验声明，需要对应版本的加载、交互、授权、执行与降级证据。冻结 v0.1 benchmark 只作为历史版本证据保留。完整正式规范见 [REQUIREMENTS.md](./REQUIREMENTS.md)。
