# 想清楚 · Think It Through 产品需求文档

> 版本：v0.2.0
>
> 文档状态：正式实现与验收依据
>
> 更新日期：2026-08-29

本文档是 v0.2.0 的唯一正式行为、安全与验收依据。产品说明、示例、优化方案或 Adapter 文档如与本文档冲突，以本文档为准。

## 1. 产品定义

### 1.1 基本信息

- **中文名**：想清楚
- **英文名**：Think It Through
- **对外完整名称**：想清楚 · Think It Through
- **Skill 命令**：`/think-it-through`
- **仓库名**：`think-it-through-skill`
- **产品形态**：面向 AI 使用者的开源思考、证据与决策支持 Skill
- **当前发布入口**：Claude Code 中显式调用；纯文本协议是跨宿主基线，其他宿主能力按 Adapter 与对应运行证据单独声明

### 1.2 一句话定位

当用户准备让 AI、自己或团队对一个重要方向投入资源时，先识别表面任务背后的真实目的，把它收敛为本轮真正需要决定的问题，再判断缺的是用户答案、外部事实、独立参与还是真实世界反馈，最终交付可执行、可复判的解决方案。

### 1.3 核心承诺

> 不替用户决定，帮助用户先想清楚，并让现实结果能够修正判断。

一次完整使用应帮助用户：

1. 看清真正想实现或保护的结果；
2. 发现表面动作与真实目的是否错位；
3. 识别本轮真正需要作出的决定；
4. 透明确认本轮有增量的思考角度；
5. 只回答一个最可能改变决定的问题；
6. 分清未知应由用户、外部证据、Agent、真人还是现实行为回答；
7. 必要且获授权时，以有界调研或独立参与补充决定敏感证据；
8. 获得一个综合、明确或条件化的判断；
9. 形成一个围绕核心假设、可验证且尽量可撤回的主现实证据闭环；
10. 留下可复制的决策快照，支持继续、调整、暂停或停止的后续复判。

### 1.4 v0.2.0 目标

v0.2.0 必须做到：

- 以宿主无关的 Portable Decision Core 定义状态、意图、等待与完成语义；
- 默认当前主 Agent、零外部调用、对话内快照和不改变外部世界；
- 目的不清时只对齐当前最大的一个未知，不发明动机或虚假排他约束；
- 可并存目的先合并、不默认排序；
- 目的与决定清楚后，以“稳定 ID + 正式名称 + 推荐状态 + 当前价值”呈现少量方法候选；
- 推荐不冒充确认，只有用户本轮提交的选择或明确文字形成唯一最终组合；
- 只执行用户确认的方法，并综合为一个决策敏感答案槽；
- A 回答后才路由 Evidence Gate、Participation Gate、真人参与或现实实验；
- Gate 只在有明确决策增量且取得对应授权时出现，失败或拒绝后不阻塞 B；
- 多 Agent 严格服从包含主 Agent 的用户总参与上限，禁止递归委派和投票综合；
- 真人掌握的价值、权责、承诺、行为意愿与专业责任不由 Agent 代答；
- 区分能力调用、参与 / 委派、私有数据访问和外部行动四类授权；
- 逐项协商能力，`unknown` 不得当作可用，真实执行产生诚实 receipt；
- B 给一个判断、一个主现实证据闭环、一个决策快照和四项反馈入口；
- 纯文本路径完整保留核心语义；Claude Code 和 ChatGPT Adapter 只映射当前会话观察到的真实能力；
- 发布支持范围、内部评测状态与具体会话执行记录分层管理，不让任何一层替另一层作证。

### 1.5 非目标

v0.2.0 不建设：

- 固定 AI 高管、永久专家团或展示型多 Agent 会议；
- Agent 多数投票或用模型一致性替代证据；
- 默认联网、无范围或无停止条件的研究平台；
- 让普通用户从完整方法目录或 Agent 拓扑中自行搭建工作流；
- 以人物、作者、思想流派、角色数量、Agent 数量、调用次数或报告长度证明价值；
- 默认读取全部私有数据、保存长期人格画像或隐藏思维链；
- 未经授权自动发送、发布、购买、删除、联系或修改外部世界；
- 把 ChatGPT 或其他宿主的 Adapter 当作原生兼容认证，或由宿主名称推定当前会话能力；
- 医疗、心理、法律、投资等持证专业意见替代品；
- 泛化的人生顾问、操控工具或对第三方内心的确定性推断器。

## 2. 用户与高价值场景

### 2.1 首要用户

首要 ICP 是资源有限、行动自主权高、没有完整研究与反方审查团队的人：

| 用户类型 | 典型处境 |
| --- | --- |
| 一人公司、小微企业经营者 | 同时承担产品、营销、销售、交付和经营判断 |
| 独立开发者、小团队负责人 | 产品、技术、团队和商业约束相互牵制 |
| AI 原生经营者与创作者 | 容易让 AI 从模糊意图直接进入高质量执行 |

职业、团队、合作、关系、高成本选择和已执行事项继续属于能力覆盖范围，但不与商业 ICP 同时作为早期传播主叙事。

### 2.2 首要 JTBD

当用户准备投入时间、金钱、声誉、关系或机会成本时，Skill 应帮助用户：

- 确认眼前动作是否服务真实目的；
- 看清当前真正要决定什么；
- 识别最可能改变决定的证据；
- 以最低充分成本获得证据；
- 得到一个综合方向，而不是框架展示或观点拼盘；
- 用现实结果复判，不因沉没成本自动继续。

### 2.3 场景范围

| 场景 | 典型决定 |
| --- | --- |
| 商业与产品 | 是否值得做、先开发还是先验证、进入哪个市场、继续扩大还是收缩 |
| 职业 | 接受哪个机会、转型还是积累、现在离开还是继续 |
| 团队与组织 | 招人还是调整分工、推进还是暂停、谁拥有决定权和承诺 |
| 合作 | 是否合作、如何分配责任与收益、何时重新协商或退出 |
| 关系 | 继续投入、调整边界、暂停接触或结束关系 |
| 高成本、低可逆选择 | 大额投入、迁移、长期承诺、重大方向变化 |
| 已执行事项 | 依据结果继续、调整、暂停或停止 |

### 2.4 不适用场景

以下情况通常不进入完整流程：

- 单纯事实查询或方法定义；
- 用户已经决定、规格清楚、低风险且可撤回的执行；
- 纯创作、娱乐或不涉及重要取舍的请求；
- 仅需技术故障分析、FMEA、代码审查、调研或排期，且没有待决用户选择；
- 紧急安全事件，此时应立即给保护指引。

## 3. 产品架构与统一术语

### 3.1 四层产品模型

```text
Decision Core（决定层）
→ Evidence Policy（证据层）
→ Participation Policy（参与层）
→ Host & Action Adapter（宿主与行动层）
```

| 层 | 职责 | 不承担 |
| --- | --- | --- |
| Decision Core | 目的、决定、方法、关键变量、判断、主实验、完成语义 | 专有工具调用和宿主推断 |
| Evidence Policy | 决定证据来源、调研范围、停止条件和证据质量 | 替用户决定价值或承诺 |
| Participation Policy | 决定单 Agent、额外 Agent 或真人参与 | 以数量、角色或投票制造权威 |
| Host & Action Adapter | 映射控件、搜索、文件、Agent、持久化和外部行动 | 改写核心等待、授权和证据语义 |

调研、多 Agent、真人参与、持久化和宿主适配不是分析方法，不进入方法注册表。

### 3.2 问题模型

```text
表面任务
→ 真实目的
→ 决策问题
```

- **表面任务**：用户直接要求 AI 做的事；
- **真实目的**：用户希望借此得到或保护的结果；
- **决策问题**：本轮真正需要作出的选择。

若表面任务不能有效服务真实目的，Skill 应指出错位，不得用执行质量掩盖方向问题。尚未确认的目的必须标为暂定，不得替用户发明动机。

### 3.3 单议题原则

一次完整分析只围绕一个核心议题。用户提出多个独立问题时，收敛到当前影响最大、最紧迫或能统摄其他问题的一个决定；其余内容作为约束或后续议题，不在同一轮展开。

### 3.4 唯一决策关键问题

唯一决策关键问题是：

> 在当前信息下，哪个答案一旦不同，最可能改变方案排序、行动方向或是否继续？

它必须：

- 直接对应本轮决定；
- 指向决策敏感度最高的未知；
- 不同答案能导向不同判断或下一步；
- 优先询问只有用户掌握的目标、约束、底线或已有事实；
- 只索取一个独立答案；
- 不包含并列、嵌套或隐蔽子问题。

### 3.5 分析方法

- **基本分析**：始终执行，不可取消，不计入候选数量；
- **核心方法**：双向钢人、失败预演；
- **专项方法卡**：对象校准、系统瓶颈、阶段匹配、资源支点、边界契约、沟通匹配、证据闭环；
- **判断视角**：内部动态检查维度，不是人物角色、Agent 或用户方法目录。

方法路由顺序为：

```text
基本分析是否足够
→ 双向钢人
→ 失败预演
→ 至多一张填补独特缺口的专项卡
→ 去重
```

基本分析之外保留 0～3 项；专项方法通常 0～1 项。零张专项方法合法。

### 3.6 结构化方法候选

R-method 的正式候选必须具有：

```json
{
  "id": "two-sided-steelman",
  "label": "双向钢人",
  "description": "用相近证据标准比较当前方向与最强替代方向，帮助判断哪条路径更值得先验证。",
  "recommended": true
}
```

要求：

- `id` 稳定、唯一、kebab-case；
- `label` 使用注册表中的正式名称；
- `description` 说明当前议题中的独特价值，不只重复名称；
- `recommended` 为布尔值；
- 推荐集合与标记一致；
- 推荐不等于确认；
- 同一候选集合中 ID 与名称不得重复；
- 不得先在正文完整重复说明，再在原生选项中只列名称。

非方法选择和旧冻结证据可保留字符串 option；v0.2.0 R-method 的正式原生推荐必须使用结构化字段。

### 3.7 证据状态与分歧类型

重要依据必须区分：

- **已确认事实**：由用户明确提供或可靠来源支持；
- **合理推断**：从事实推导的解释；
- **待验证假设**：判断依赖但尚无充分证据；
- **关键未知**：当前无法确认且可能改变决定。

分歧可能来自：

- 事实；
- 因果或预测；
- 目标或价值；
- 资源与约束；
- 风险偏好。

价值选择不得伪装成事实结论；模型记忆、搜索摘要、方法来源或 Agent 一致性不得冒充已确认事实。

### 3.8 判断状态

**尚未执行：**

- 暂不行动（`hold`）；
- 小步验证（`small_test`）；
- 有条件推进（`proceed_conditionally`）；
- 可以推进（`proceed`）。

**已经执行：**

- 继续（`continue`）；
- 调整（`adjust`）；
- 暂停（`pause`）；
- 停止（`stop`）。

### 3.9 四类授权

授权类型稳定为：

1. `capability_call`：调用具体工具或能力；
2. `participation_delegation`：增加 Agent 或请求真人参与；
3. `private_data_access`：读取具体私有资源；
4. `external_action`：发送、发布、购买、删除或修改外部世界。

四类授权互不继承。方法确认、B 反馈、Agent 上限和历史偏好都不能推定任何授权。授权必须记录用途、操作、资源、数据边界、排除范围和有效期。

### 3.10 能力协商与回执

每个会话逐项记录：

```text
interaction.free_text
interaction.select_one
interaction.select_many
search.public_web
search.private_corpus
tools.read
tools.write
agents.subagent
agents.parallel
humans.request_review
persistence.session
persistence.case
permissions.tool_call
permissions.private_data
permissions.external_action
fallback.text
```

每项能力同时记录：

- 可用性：`available / unavailable / unknown`；
- 就绪状态：`ready / requires_approval / requires_auth / failed`；
- provider、限制和状态证据。

`unknown` 不得当作可用。真实执行按 `core/receipts.schema.json` 记录 provider、范围、来源、计划与实际状态、权限使用、失败和降级；receipt 不保存隐藏思维链。

## 4. 状态与等待合同

### 4.1 唯一状态流

```text
空状态
→ R-align
→ R-method
→ A
→ Gate-routing
→（必要且获授权时）Evidence Gate / Participation Gate
→ B
→ 反馈 / 结束 / 新一轮 R
```

Gate 是条件能力，不是固定第四阶段。不必要、不可用、未授权、被拒绝或失败时，记录后基于已有信息进入条件化 B。

R、A、B 和 Gate 的职责不得混合。轻量呈现只能压缩篇幅，不能改变输入、输出或等待边界。

### 4.2 空状态

用户没有给出具体议题时，只发出一次开放邀请并等待：

> 请说出你此刻最想想清楚的一件事。
>
> 它可以是一个问题、想法、困惑、选择、计划，甚至是一种“哪里不对但说不清”的感觉。

不得自行编造议题、展示方法菜单、搜索、委派或发分类问卷。

### 4.3 R-align：目的对齐

当系统不能在不发明动机的前提下同时说清希望得到或保护什么以及本轮决定时：

1. 自然承接并将当前理解标为暂定；
2. 识别当前最大的一个目的槽；
3. 答案可并存时给 2～4 个贴合议题、平行、同层级的结果方向并使用多选；
4. 真实边界使答案有限且互斥时使用单选；
5. 无法预造有限答案时直接开放回答；
6. 说明可通过宿主自由输入补充、纠正或拒绝；
7. 等待用户表达，不展示方法、判断、行动或 Gate。

用户回答后先合并可并存目的，不默认排序。只有以下条件全部成立才再澄清一次：

- 用户明确给出真实排他约束；
- 多个目的在该约束下不能同时满足；
- 不同优先级会改变“是否做、选哪条路或是否继续”；
- 合并或条件化表达不能消除冲突。

系统不得自行制造“只能做一件事”“第一版必须放弃其他结果”或“只能验证一个目标”。

### 4.4 R-method：方法推荐与确认

目的和决定足够清楚后：

1. 轻量判断事项阶段、影响、错误成本、时间压力、可逆性和主要分歧；
2. 按最小路由筛选 0～3 项候选；
3. 每个候选包含稳定 ID、正式名称、当前价值和推荐状态；
4. 支持 option description 时在同一原生选项内呈现；不支持时使用紧邻说明；文本降级保留全部字段；
5. 说明基本分析始终包含；
6. 允许加入、取消、替换或纠正；
7. 停止并等待本轮唯一最终组合。

调整语义稳定为：

- “加入 X”保留当前组合并增加 X；
- “取消 X”只移除 X；
- “用 X 替换 Y”才同时移除 Y、增加 X；
- 自由文字改变目的或议题时回到 R-align 或重新路由；
- 提交后自然回显最终组合，不展示内部 ID；
- 未确认或取消的方法不得暗中执行。

推荐、默认勾选、打开控件、浏览候选、补充背景、历史偏好或“你看着办”不构成方法确认。明确按推荐继续、明确只做基本分析、明确调整后形成唯一组合或提交最终集合才构成本轮确认。

### 4.5 R 的共同边界

R-align 与 R-method 均不得：

- 正式执行双向钢人、失败预演或专项方法；
- 给判断、行动、验证、保护或停止方案；
- 提出 A 的唯一决策关键问题；
- 调用、请求或建议网页、文件、数据、账号、子 Agent 或真人参与；
- 将方法选择推定为能力、参与、数据或外部行动授权。

### 4.6 A：执行已确认方法并提问

输入：一个已收敛议题、本轮确认的方法组合和用户已提供背景。

系统行为：

1. 始终执行基本分析；
2. 自然回显最终确认角度；
3. 只执行已确认方法，每项产生独特价值；
4. 去重并综合，不逐卡汇报、不模拟专家会议；
5. 重述真实目的和本轮决定；
6. 形成竞争判断、关键失败机制、真正分歧和一个关键变量；
7. 索取一个独立答案；
8. 问后立即等待。

交互规则：

- 答案天然有限、互斥且不显著锚定时，实际调用原生单选；
- 答案开放时不调用选择控件、不预造答案；
- 两条路径都只有一个答案槽；
- Claude Code 当前合同下，用户可见正文和原生问题文本合计只有一个问号，且问题位于最后；
- A 可以复用用户已有同值、同单位数字，不得自行植入样本量、期限、金额、人数、比例、阈值或未来投入长度。

A 不得给最终判断、行动、验证方案、Gate 建议、授权请求、外部调用或自行假设用户答案。

### 4.7 Gate-routing

用户正常回答、不完整回答、“不知道”或拒绝回答后，不再追问同类信息。关键未知按来源路由：

| 未知类型 | 处理 |
| --- | --- |
| 用户价值、底线、风险承受 | 保留答案或未知，Agent 不代答 |
| 公开、时效性强且会反转判断的事实 | 评估 Evidence Gate |
| 不重复、可验证、可停止的独立任务 | 评估 Participation Gate |
| 权责、承诺、客户行为或专业责任 | 生成最小真人参与材料，默认不发送 |
| 只能由现实反应回答 | 留给 B 的主现实证据闭环 |
| 其他 | 直接进入 B |

默认零外部调用、单 Agent。不能为展示能力而进入 Gate。

### 4.8 Evidence Gate

#### 进入条件

以下条件必须全部成立：

1. 未知是外部可验证事实，不是用户价值、底线或真实承诺；
2. 至少两个合理答案会改变方案排序、是否继续或投入边界；
3. 事实具有市场、地区、时间、法规、价格、政策或替代方案依赖；
4. 获取方式明确、范围受限且可停止；
5. 预期证据价值高于成本、延迟和隐私风险；
6. 能力状态不是 `unknown`，并已就绪或可取得授权；
7. 已取得本次具体 `capability_call` 授权，私有数据和外部行动另行授权。

任一条件不满足即直接进入 B。

#### 启用前合同

在同一连续理解与选择单元中说明：

- 要改变的决定；
- 关键证据问题；
- 市场、地区、时间和对象范围；
- 停止条件；
- 来源要求；
- provider、只读状态和数据边界；
- 相对成本与延迟；
- 失败降级。

取得用户具体授权后才执行。用户拒绝或收窄范围时立即遵守，不得换工具绕过。

#### 执行与结果

一次调研只服务一个决定敏感证据问题，并记录：

```text
要改变的决定
关键证据问题
范围
停止条件
来源要求
支持证据
反对证据
冲突与空白
证据日期
对判断的影响：改变 / 不改变 / 仍不确定
```

一手来源优先；主张对应具体来源和日期；相同来源的多个转述只算一份证据；搜索摘要不自动等于原始来源；必须主动寻找反对证据和替代解释。

能力不可用、被拒绝、认证失败、来源冲突或无可靠答案时，保留未知、降低承诺、记录 receipt 并进入 B，不虚构结果或无限搜索。

### 4.9 Participation Gate

#### 多 Agent 进入条件

默认当前主 Agent。只有存在至少一个不重复、可验证、可停止的独立任务，并具有下列增量之一时才建议额外 Agent：

- 不同市场、来源或法规可并行检索；
- 候选解释需独立形成以降低共享锚定；
- 需要专门尝试推翻候选判断；
- 需要独立审计来源、冲突和遗漏；
- 大规模研究的并行可明显减少等待时间。

目的未清、任务重复、价值由用户掌握、成本高于增量或能力 `unknown` 时保持单 Agent。

#### 用户边界与数量公式

用户可设置：

```text
协作增强：关闭 / 按需询问 / 在上限内自动
总参与 Agent 上限：包含当前主 Agent
偏好范围：仅本次 / 本会话 / 保存为默认
```

默认建议“按需询问”；保存为默认必须由用户主动开启。

```text
可用额外 Agent 上限 = max(0, 用户总参与上限 - 1)

实际额外 Agent 数 = min(
  独立增量任务数，
  可用额外 Agent 上限，
  产品额外 Agent 安全上限，
  宿主可用额外 Agent 上限，
  成本与延迟预算允许数量
)

实际总参与 Agent 数 = 1 + 实际额外 Agent 数
```

用户上限是硬上限，不是使用目标。数量表达必须同时给出主 Agent、额外 Agent 和总参与数。

#### 启用前合同

在一个互斥选择中说明：

- 每个额外 Agent 的具体、不重复任务；
- 当前主 Agent、建议额外数和总数；
- 用户上限及其限制；
- 按 Agent 裁剪的数据、明确排除的数据；
- 公开联网、私有数据和外部行动各自状态；
- 相对调用量、成本和可能延迟；
- Agent 失败后的降级；
- 按建议启用、降低数量或保持单 Agent的选择与自由收窄入口。

用户提交后形成 `participation_delegation` 授权；推荐或默认选中不构成授权。

#### 执行与综合

每个额外 Agent 只接收最小上下文，禁止递归委派，并返回：

```text
assigned_question
claims
evidence_and_sources
assumptions
uncertainties
conflicts
what_would_reverse_this
```

主 Agent 必须：

- 去重同一来源和事实链；
- 不按多数票决定；
- 不把模型一致性当现实证据；
- 定位事实、预测、价值、约束或风险分歧；
- 标明采用、舍弃和仍冲突的材料；
- 最终给一个综合判断和一个主现实证据闭环。

任何 Agent 失败都不得阻塞 B。回执必须记录计划、实际启动、完成、失败和实际总数，以及数据、能力、关键冲突和缺失工作。

#### 真人参与

下列内容必须路由给对应真人：

- 决策权和预算承诺；
- 受影响者的价值与底线；
- 团队真实权责和执行承诺；
- 客户真实付款或行为意愿；
- 合作方边界和退出条件；
- 持证专业意见与责任。

真人参与材料至少包含：

```text
为什么需要这位参与者
只请对方回答什么
需要共享哪些最小背景
哪些私有内容不共享
回答如何改变判断
由谁负责发送和收集
```

默认 `draft_only`。发送、邀请、建群、预约或联系必须取得独立 `external_action` 授权。真人未回复时保留缺口，不冒充对方意愿。

### 4.10 B：判断、主实验、快照与反馈

无论 Gate 未进入、被拒绝、失败或完成，最终都进入 B，不继续盘问。

B 必须包含：

1. 一个与事项阶段匹配的正式判断状态；
2. 推荐方向与关键依据；
3. 事实、推断、假设和未知的边界；
4. 判断成立条件与反转信号；
5. 一个围绕同一核心假设的主现实证据闭环；
6. 调研、Agent 或真人材料的来源、冲突和回执（如实际发生）；
7. 可复制的决策快照；
8. 四项反馈入口。

主现实证据闭环必须包含：

- `core_hypothesis`：一个核心假设；
- `action`：只做什么及必要边界；
- `observation`：什么现实信号支持或反对继续；
- `reassessment`：何时或依据什么重新决定。

动作、观察和复判可以包含必要连续操作，但必须共享同一核心假设、资源边界、观察标准和复判点。多个无关假设或项目不得包装成一个实验。

用户回答“不知道”、不完整或拒绝回答时，把信息不足纳入判断，通常降低承诺或提高可逆性，不再追问。

数字优先沿用用户输入。系统新增的每个决定相关数字必须在所在句或局部列表项标为“建议边界”“启发式起点”或给可靠来源；一处全局免责声明不能替其他数字背书。

### 4.11 DecisionRecord

B 默认在对话中输出符合 `core/decision-record.schema.json` 语义的快照，至少包括：

- 议题、真实目的、本轮决定和已确认方法；
- 判断、理由和成立条件；
- 事实、推断、假设、未知和来源；
- 反转信号；
- 主现实实验；
- 复判触发；
- 主 / 额外 Agent 实际数量、能力、consent 和 receipt；
- 持久化状态。

默认：

```json
{
  "mode": "conversation_only",
  "authorized": false
}
```

写入文件或远端必须提供具体 `destination` 和授权 ID。不得保存隐藏思维链。用户粘贴或授权读取旧快照后，复判应区分判断错误、执行偏差、资源错配和条件变化，并更新判断与快照。

### 4.12 B 反馈

完整判断、主实验和快照之后，实际调用一次四项原生单选：

- `accept`：方向符合我；
- `adjust-next-step`：调整下一步；
- `disagree`：不同意这个判断；
- `set-aside`：暂时先放一放。

反馈问题只收集对已完成内容的方向，不索取预算、期限、证据或新决策信息。

补充模式：

- 实际观察到与选择并存的独立附注 → `native-note`；
- 原生单选存在但没有独立附注 → `follow-up-message`；
- 文本降级 → `inline-text`。

宿主 `Other` 是替代式自由回答，不是独立备注。选择与文字冲突时以文字为准。

`accept` 或 `set-aside` 且无冲突补充时结束；`adjust-next-step` 或仅修改实验时开启新一轮 R 并保留判断；`disagree`、纠正或新事实开启新一轮 R 并重检判断。

反馈不得执行实验，也不得授权能力、委派、私有数据、持久化或外部行动。

## 5. 交互与表达合同

### 5.1 原生交互与文本降级

宿主提供等价选择工具时必须实际调用：

- `compatible-set` → 原生多选；
- `finite-mutually-exclusive` → 原生单选；
- `open` → 自由回答，不调用选择控件；
- B → 固定原生单选。

只有控件不可用、调用失败或被拒绝时才文本降级，不原样重试。failed / rejected 记录已发生的调用 trace，surface 记录最终呈现。

文本降级使用普通编号，不用 Markdown checkbox、任务列表、`○`、HTML radio 或其他伪控件。产品不自建“其他/Other”。

每轮最多一个选择问题。R-method 的一个原生控件可以在同一选项内完成方法理解与最终组合提交，不要求再加一轮“推荐确认”。

### 5.2 信息关系驱动排版

- 段落：一个完整观点、承接或解释；
- 有序列表：步骤、依赖、优先级或编号回复；
- 无序列表：平行事实、理由、风险与限制；
- 标签—值：短字段、授权范围、协作计划与回执；
- 表格：多个对象的同维度横向比较；
- 标题：多个需要独立扫描的内容块。

一意一段。承接、暂定理解、必要解释、操作提示和正式问题用空行分开；正式问题独立位于最后。选项短、平行、同层级。不按固定字符宽度硬折行，不为版式制造报告腔。

“单屏”是同一连续理解与选择单元，不是物理屏幕高度限制。

### 5.3 稳定身份

Skill 表现为有经验、值得信赖的思考搭档：

- 理解时有温度但不假装亲密；
- 提问克制，不扩展成问卷；
- 信息足够时给明确方向；
- 不自称朋友、导师或教练；
- 不模仿人物口吻，不模拟专家会议；
- 跟随用户语言、领域词汇和正式程度；
- 最终选择权始终属于用户。

## 6. 分析方法合同

### 6.1 基本分析

至少完成：

1. 区分表面任务、真实目的和决策问题；
2. 识别阶段、约束、可逆性和错误成本；
3. 区分证据状态和分歧类型；
4. 比较哪些变量会实际改变决定；
5. 选出唯一关键问题；
6. 在 Gate-routing 中选择正确证据与参与来源；
7. 在 B 中形成判断、反转条件、主现实证据闭环和快照。

### 6.2 双向钢人

围绕同一决定构造当前方向与最强替代方向的最强论证，并使用相近证据标准检验双方。必须说明成立条件、机制、证据、收益和代价；不得制造稻草人或强行五五开。

### 6.3 失败预演

假设候选路径已经执行并失败，倒推具体失败结果、1～3 条关键因果链、早期信号和可控边界。发现必须影响关键变量、B 判断、主实验、预警或停止条件；不得罗列抽象风险大全。

### 6.4 专项方法卡

七张专项卡保持正式名称：对象校准、系统瓶颈、阶段匹配、资源支点、边界契约、沟通匹配、证据闭环。

每张卡至少记录：稳定 ID、正式名称、白话定义、适用与不适用信号、输入与独特产出、组合关系、来源和固定版本、许可证、修改说明及适用 / 不适用 / 重叠测试状态。

第三方来源不获得事实权威；人物口吻、操控内容和不安全框架必须移除。高度重叠的方法应合并并保留来源归属。

## 7. Host Adapter 合同

### 7.1 纯文本 Adapter

纯文本是一等参考实现，不是残缺兜底。没有原生控件、搜索、子 Agent、真人协作或持久化时，仍完整保留：

- R-align、R-method、A、B 和等待边界；
- 方法正式名称、推荐状态、当前价值和最终组合；
- 四类授权与能力诚实；
- 主现实证据闭环、决策快照和 B 四项反馈；
- 搜索、Agent、真人和持久化不可用时的诚实降级。

### 7.2 Claude Code Adapter

- 当前可靠加载方式是显式 `/think-it-through`；
- 原生选择必须有实际工具调用证据；
- 搜索、文件、Agent、Workflow 和持久化逐项协商；
- 复杂 Workflow 需要用户明确选择，不能由一般 Agent consent 推定；
- 项目或用户更严格的 Agent 总额度必须同时遵守；
- 外部服务写操作需要独立确认；
- 自动发现的冻结 v0.1 holdout 为 9/16（正例 1/8、负例 8/8），不得宣称自然语言自动加载已通过。

### 7.3 ChatGPT Adapter

v0.2.0 的支持范围是 Skill-only / 纯文本语义映射。Adapter 的存在不构成 ChatGPT 原生兼容认证，也不得据此假设通用原生控件、宿主搜索、MCP、私有连接器、子 Agent、自定义 UI 或持久化。

当前会话未观察到某项能力时使用纯文本路径；观察到额外能力时，仍须逐项记录 provider、可用性、就绪状态、对应授权、实际 trace、receipt 与失败降级。扩大宿主原生兼容声明必须有对应版本化运行证据。

## 8. 功能需求

### FR-01 适用性与单议题

Skill 应判断是否需要完整流程，只处理一个核心议题；低风险、规格清楚且可撤回的任务直接回答或执行。

### FR-02 目的与决定重构

Skill 应区分表面任务、真实目的和决策问题，指出动作与目的错位，不发明用户动机。

### FR-03 R-align

目的未清时只处理当前最大的一个目的未知，按答案形态使用多选、单选或开放回答；保留自由纠正；先合并可并存目的，不默认排序，不虚构排他约束。

### FR-04 R-method 结构化推荐

目的清楚后筛选 0～3 个有增量的方法。每个正式候选必须有稳定 ID、正式名称、当前价值和推荐状态，在同一连续选择单元中理解与提交，不重复完整说明。

### FR-05 方法确认与调整

推荐不构成确认。只有本轮提交的集合或明确文字形成唯一最终组合。加入、取消、替换和文字覆盖语义稳定；未确认或取消的方法不得执行。

### FR-06 已确认方法执行

A 始终执行基本分析，只执行已确认方法，每项产生独特价值，并综合而非逐卡汇报或模拟角色会议。

### FR-07 唯一问题与数字纪律

A 只索取一个独立答案，按答案形态选择表面，问后等待；不得给判断、行动、Gate 或系统自造决定相关数字。

### FR-08 Gate-routing

A 回答后停止同类追问，将未知正确路由给用户、外部证据、额外 Agent、真人、专业人士或现实实验。

### FR-09 Evidence Gate

只有全部进入条件成立才调研；启用前说明决定、问题、范围、停止条件、来源、能力、数据、成本和降级，并取得具体能力授权。

### FR-10 证据质量与失败降级

调研应保留来源、日期、支持与反对证据、冲突、空白和判断影响。失败、拒绝、认证缺失或冲突时保留未知并进入 B。

### FR-11 Participation Gate

默认单 Agent。只有独立增量任务才建议额外 Agent，并严格按用户总上限、产品上限、宿主上限及成本预算计算实际数量。

### FR-12 多 Agent 执行与综合

额外 Agent 只接收最小上下文、不得递归委派，返回结构化材料。主 Agent 去重来源、呈现冲突并综合，不按投票。失败不阻塞 B，实际数量如实回执。

### FR-13 真人参与

价值、权责、承诺、客户行为和专业责任路由给对应真人。默认只生成最小可转发草稿；未经外部行动授权不发送或联系。

### FR-14 四类授权

能力调用、参与 / 委派、私有数据和外部行动必须独立、具体、最小、当次有效；任何选择或反馈都不得推定授权。

### FR-15 能力协商与回执

逐项记录能力可用性、就绪状态、provider、限制和证据。`unknown` 不可使用；真实操作必须记录状态、范围、来源、权限、失败和降级。

### FR-16 B 判断

用户回答后不再盘问；B 使用统一状态，给推荐方向、关键依据、证据边界、成立条件和反转信号。

### FR-17 主现实证据闭环

B 给一个围绕核心假设的动作、观察和复判。必要连续操作可以存在，但不得包含多个无关假设或项目。每个系统新增数字有局部来源或建议性质。

### FR-18 决策快照与复判

B 默认在对话中输出 DecisionRecord，不持久化。持久化需要目标和授权。新结果返回时区分判断错误、执行偏差、资源错配和条件变化，并更新状态与快照。

### FR-19 B 四项反馈

完整判断、实验和快照后实际调用四项单选，只路由用户对已完成内容的反馈。补充通道必须与宿主实际能力一致；文字优先；反馈不执行或授权。

### FR-20 跨宿主保真

纯文本完整保真；Claude Code 和 ChatGPT Adapter 只映射当前会话真实能力；任何宿主能力不可用时诚实降级，不虚构兼容或调用。

### FR-21 安全与外部行动

高风险专业事项只整理事实、问题、选项、风险和咨询重点；紧急危险优先保护。未经授权不得发布、购买、联系、删除或修改外部世界。

### FR-22 结束与重新进入

用户可随时停止。A 尚未回答时实质改题回到 R；Gate 拒绝或失败进入 B；接受或暂放结束；调整、不同意、纠正和新事实开启新一轮 R。

## 9. 非功能需求

### NFR-01 简洁与渐进披露

`SKILL.md` 保持少于 500 行；详细协议、策略、Adapter、方法和安全规则按职责放入子文件。用户只看到当前必要信息，不先配置复杂模式。

### NFR-02 宿主无关

Core 不依赖特定模型、平台、工具名、固定 Agent 数量、联网或持久化；Adapter 变化不得破坏状态、授权和等待语义。

### NFR-03 可解释与可纠错

用户能理解真实目的、方法为何推荐、判断依据、证据边界和反转条件，并能通过自由文字纠正选择，不要求重新点击。

### NFR-04 可控性

用户控制方法组合、调研范围、Agent 总上限、数据和外部行动。取消不得暗中执行；上限不得被递归委派绕过。

### NFR-05 隐私与最小披露

只请求当前判断必要的信息。私有资源按具体目的、资源和最小字段授权；额外 Agent 按任务裁剪上下文；默认不持久化，不保存隐藏思维链。

### NFR-06 失败可降级

控件、搜索、认证、Agent、真人或持久化不可用时，核心流程仍可基于已有信息完成条件化判断、现实实验、快照和反馈。

### NFR-07 可追溯与可测试

状态、交互表面、真实工具调用、结构化方法 option、consent、capability、receipt、Gate 路由、Agent 数量、非递归、真人草稿、DecisionRecord、B 反馈和文本降级必须有结构化证据或确定性检查。

### NFR-08 证据诚实

静态规范、schema、fixture、测试和线框只证明合同定义，不证明某次真实模型行为、原生 UI、搜索、Agent、跨宿主能力或用户体验。发布支持范围、内部评测状态和具体会话执行记录必须分层管理；实际能力调用只由当前会话 trace 与 receipt 建立。

### NFR-09 体验评测独立

核心 UX 使用十维 20 分 rubric；v0.2.0 增强能力使用八维 16 分 rubric。两者不得覆盖或重解释冻结 v0.1 行为 rubric。正式体验声明必须有真实 transcript、工具 trace 或用户评审证据。

### NFR-10 安全与来源治理

第三方方法可追溯到固定仓库、commit、准确文件、许可证和修改说明。关系场景不得提供操控、欺骗、跟踪、胁迫或施压策略。

## 10. 交付结构与维护边界

### 10.1 Skill 内结构

```text
skills/think-it-through/
├── SKILL.md
├── core/
│   ├── protocol.md
│   ├── intents.schema.json
│   ├── consent.schema.json
│   ├── receipts.schema.json
│   └── decision-record.schema.json
├── policies/
│   ├── evidence-routing.md
│   └── participation-routing.md
├── adapters/
│   ├── text.md
│   ├── claude-code.md
│   └── chatgpt.md
├── references/
│   ├── methods/
│   │   ├── registry.yaml
│   │   └── *.md
│   └── *.md
├── evals/
├── LICENSE
└── THIRD_PARTY_NOTICES.md
```

`skills/think-it-through/` 是唯一维护源。`.claude/skills/think-it-through/` 只是被忽略的本地安装副本。

### 10.2 Schema 合同

v0.2.0 schema 必须：

- 使用 JSON Schema Draft 2020-12；
- `$id` 包含 v0.2.0；
- 使用稳定枚举；
- 对对象使用 `additionalProperties: false`；
- 对持久化、数量、授权和状态关系提供条件约束；
- 不仅为 schema 校验引入新运行时依赖。

### 10.3 分发包

独立 `.skill` 包必须包含 `SKILL.md`、core、policies、adapters、必要 references、MIT 许可证和第三方通知；排除 evals、benchmarks、历史 transcript、workspace、缓存、私有数据和本机配置。当前分发集合为 28 个源文件。构建必须使用新的空目录，并解包复验文件集合与字节内容。

### 10.4 冻结历史证据

不得用 v0.2.0 当前合同重新评分或改写：

- `scripts/grade_contracts_v0_1.py`；
- `scripts/grade_behavior_runs.py` 的 legacy 导入与评分语义；
- `benchmarks/behavior-v0.1/`；
- `benchmarks/trigger-v0.1/`；
- 冻结 transcript、评分、语义 rubric 和 description。

`scripts/test_legacy_behavior_grader.py` 是保护上述冻结语义的可维护回归测试，可适配 canonical benchmark 的只读输入布局；任何调整都不得改变 transcript、冻结评分重现、legacy import 或受保护哈希。

当前发布入口仍为 `/think-it-through`。冻结 discovery holdout 为 9/16：正例 1/8、负例 8/8。不得把自然语言自动加载描述为已通过。

## 11. 验收标准

### AC-01 空状态与适用性

无具体议题时只发开放邀请并等待。低风险清晰任务不强行进入完整流程；多个独立议题收敛为一个决定。

### AC-02 目的对齐

R-align 不发明动机；选项只服务一个目的未知；按答案形态使用 multi / single / none；可并存目的先合并、不默认排序；没有真实排他约束时不强迫排序。

### AC-03 原生交互真实性

宿主控件可用时观察到真实工具调用；只有 unavailable / failed / rejected 时才文本降级；Markdown 或线框不得冒充调用。

### AC-04 文字优先与 Other 边界

选择和文字冲突时采用文字并重新路由；拒绝候选不要求重选；产品不创建 Other；宿主 Other 不被当作低优先级候选或独立备注。

### AC-05 结构化方法 option

v0.2.0 原生方法候选均具有稳定 ID、正式 label、足够具体的 description 和布尔 recommended；推荐集合与标记一致；ID 和名称唯一。

### AC-06 单屏内联方法体验

正式名称、推荐状态、当前价值和选择入口位于同一连续单元；正文不完整重复 description；非推荐项仍相关且有独特增量；不为凑数量加入无关候选。

### AC-07 推荐不等于确认

推荐、默认勾选、浏览、历史偏好和补背景均不构成确认。只有本轮提交或明确文字形成最终组合，确认后自然回显。

### AC-08 调整与执行一致

加入不移除原组合，取消只移除指定方法，替换才同时增删。A 实际使用组合与 R 最终组合一致；取消的方法无变相产出。

### AC-09 方法质量与综合

双向钢人对称且不稻草人化；失败预演包含具体机制和早期信号；专项卡产生独特价值。结果去重并综合，不逐卡汇报、不模拟会议。

### AC-10 A 只问一个

A 只有一个答案槽，按答案形态使用原生单选或自由回答，问后等待。Claude Code 当前合同下只有一个末尾问号。没有判断、动作、Gate 建议、外部调用或系统数字锚点。

### AC-11 Gate-routing 正确

A 回答后不再追问同类信息；用户价值不被搜索或 Agent 代答；外部事实、独立任务、真人责任和现实行为被路由到正确来源。

### AC-12 Evidence Gate 必要性

只有决定敏感、外部可验证、时效或范围相关、获取有界且价值高于成本时进入。普通分析或现实实验足够时保持零调用。

### AC-13 Evidence consent 与范围

启用前明确决定、问题、范围、停止条件、来源、provider、数据、成本和降级，并取得本次具体 capability consent；私有数据和外部行动分开。

### AC-14 Evidence 质量

调研结果含具体来源、日期、支持与反对证据、冲突、空白和判断影响；相同来源去重，不按链接数量投票。

### AC-15 Evidence 失败降级

调用失败、拒绝、认证缺失、来源冲突或无可靠答案时，receipt 如实记录，未知保留，承诺降低并进入 B，不虚构或无限搜索。

### AC-16 Participation 必要性

只有不重复、可验证、可停止的独立任务有明确增量时建议额外 Agent；简单、重复、价值问题或成本不相称时保持单 Agent。

### AC-17 Agent 上限

实际额外数符合公式；总数等于 1 + 实际启动额外数；不超过用户、产品、宿主和预算限制；无独立任务时不为用满额度创建 Agent。

### AC-18 Participation consent 与可见性

启用前显示具体任务、主 / 额外 / 总数、数据、成本延迟和降级，允许降低数量或保持单 Agent。用户提交前不委派。

### AC-19 Agent 最小上下文与非递归

每个 Agent 只获任务所需内容并返回规定字段；额外 Agent 不得递归委派或复制完整私有会话。

### AC-20 综合而非投票

主 Agent 去重来源、定位冲突、标明采用与缺失材料，最终形成一个综合判断。多数票、角色权威或模型一致性不得提高置信度。

### AC-21 协作回执与失败

receipt 中计划、启动、完成、失败和实际总数关系一致。任何 Agent 失败都如实呈现并降级继续，不补造结果或阻塞 B。

### AC-22 真人参与边界

价值、权责、承诺、客户意愿和专业责任不由 Agent 代答；默认只生成包含最小背景和明确问题的可转发草稿；未授权不发送。

### AC-23 四类授权分离

能力、参与、私有数据和外部行动的测试中，任何授权不推定另一种；方法选择和 B 反馈不扩大授权。

### AC-24 能力协商

每项能力有稳定可用性和就绪枚举、provider 与证据；`unknown` 不被使用；未调用不生成虚假回执。

### AC-25 B 判断合同

B 包含阶段匹配判断、推荐方向、关键依据、证据边界、成立条件和反转信号；用户回答不知道或拒绝时仍给条件化判断，不继续盘问。

### AC-26 主现实证据闭环

B 只有一个核心假设，动作、观察和复判共享资源边界与复判点。必要连续动作合法；多个无关假设或项目必须失败。

### AC-27 数字纪律

用户数字按同值同单位复用；系统新增的每个决定相关数字都有局部建议性质或可靠来源，不由全局免责声明代替。

### AC-28 DecisionRecord

快照包含核心字段、事实 / 推断 / 假设 / 未知 / 来源、反转信号、主实验、复判和参与能力记录；默认 `conversation_only` 且未授权。

### AC-29 持久化授权

写文件或远端的记录必须有 `authorized=true`、具体 destination 和 consent ID；无授权不写入，不声称已保存。

### AC-30 B 四项反馈

完整判断、主实验和快照后实际调用四项 single。问题只路由反馈；补充模式与宿主证据一致；文字优先；反馈不执行、不形成 Gate 或授权。

### AC-31 纯文本保真

无原生控件、搜索、Agent 或持久化时，文本路径仍完整保留方法、授权、判断、主实验、快照和四项反馈，并使用普通编号而非伪控件。

### AC-32 Claude Code 适配诚实

只使用当前会话观察到的工具；失败或拒绝保留 trace 并降级；Workflow 只在用户明确选择时使用；冻结 discovery 结果被准确陈述。

### AC-33 ChatGPT 支持边界

Adapter 只保证 Skill-only / 纯文本语义映射，不被描述为 ChatGPT 原生兼容认证。当前会话没有能力证据时使用文本路径；任何原生 UI、搜索、MCP、私有连接器、Agent 或持久化调用都必须逐项记录 provider、状态、授权、trace、receipt 与降级。

### AC-34 跨场景、安全与用户拍板

商业、职业、团队、合作、关系、高成本和已执行事项遵守同一合同并使用对应语言；高风险不冒充专业意见；紧急危险优先保护；最终决定归用户。

### AC-35 版本、评测与冻结隔离

current schema、fixtures、grader、UX 和公开文档均为 v0.2.0；legacy grader 和冻结 v0.1 benchmark 未被改写；旧分数不用于证明 v0.2.0。

### AC-36 分发集合

打包文件包含 `SKILL.md`、core、policies、adapters、必要 references、许可证和第三方通知，当前严格为 28 个源文件；不包含 evals、benchmarks、examples、workspace、缓存或本机配置；解包内容与源码逐字节一致。

## 12. 严重失败判定

出现以下任一项，视为严重失败：

- 空状态编造议题、搜索或给建议；
- R-align 发明用户动机、虚假排他或强迫可并存目的排序；
- R 提前展示正式分析、判断、行动、Gate 或外部调用；
- 宿主控件可用时只输出 Markdown 或伪控件；
- multi / single / none 使用错误，或失败后反复重试而不降级；
- 产品自建 Other，或把宿主 Other 冒充独立备注；
- 选择与文字冲突时不采用文字；
- R-method 缺少方法 ID、正式名称、当前价值或推荐状态；
- 把推荐、默认勾选、历史偏好、打开控件或补背景当作确认；
- 被取消方法仍暗中执行，加入静默移除已有方法，或 R / A 组合不一致；
- 方法只有名称没有独特产出，钢人稻草人化，预演只是风险标签；
- A 有多个答案槽、问后继续、提前判断或加入用户未给出的决定相关数字；
- A 之前调用或请求搜索、文件、Agent 或真人参与；
- 搜索或 Agent 代替用户价值、真实承诺、客户行为或专业责任；
- 未满足进入条件仍启动 Evidence 或 Participation Gate；
- 未获具体授权调用能力、增加 Agent、读取私有数据或改变外部世界；
- 公开联网、参与、私有数据和外部行动授权相互继承；
- 用户总参与上限被超过，或额外 Agent 递归委派绕过上限；
- 用 Agent 数量、模型一致性、多数票或相同来源转述替代证据；
- 能力 `unknown`、不可用、拒绝、失败或认证缺失时仍声称完成；
- 虚构来源、日期、能力、Agent 状态、真人意见、持久化或外部行动；
- Gate 失败后阻塞 B、无限搜索或补造结果；
- B 没有明确判断、反转条件、一个主现实证据闭环或决策快照；
- 多个无关假设或项目被包装成一个实验；
- B 继续盘问新的决策信息；
- B 缺少四个稳定反馈方向，错误承诺独立附注，或把反馈当执行 / 授权；
- 未经授权写入文件、保存长期偏好、发送、发布、购买、联系、删除或修改；
- 纯文本降级删除核心语义，或把静态 ChatGPT Adapter 当作实测兼容；
- 关系场景给操控、欺骗、跟踪、胁迫或施压策略；
- 高风险专业领域冒充持证意见，或紧急危险仍延长流程；
- 用静态 schema、fixture、单元测试或旧 v0.1 分数宣称 v0.2.0 真实能力或体验通过；
- 没有把最终选择权交还用户。

## 13. 评测设计与证据声明

### 13.1 评测层次

仓库严格区分：

1. **发现性**：description 是否加载并避开负例；
2. **加载后行为**：真实模型是否遵守多轮合同；
3. **核心用户体验**：对话是否帮助用户对齐、纠错、理解和行动；
4. **增强能力体验**：调研、参与、跨宿主、解决方案和复判是否带来真实增量。

### 13.2 current fixtures 最低覆盖

v0.2.0 current fixtures 至少覆盖：

- 空状态、R-align、R-method、A、B 和反馈转移；
- 可并存目的、真实互斥边界和开放回答；
- 结构化方法 options、推荐不等于确认、加入 / 取消 / 替换；
- 原生调用、失败 / 拒绝 / unavailable 降级、Other 和文字优先；
- A 单答案槽、数字防锚定；
- B 主现实闭环、数字局部来源和四项反馈能力矩阵；
- Evidence Gate 必要 / 不必要路由、consent、receipt、来源、日期、反证和失败；
- Participation Gate 上限、非递归、最小上下文、回执、失败和无投票综合；
- 真人参与默认草稿；
- 四类授权分离；
- text / Claude Code / ChatGPT Adapter；
- DecisionRecord 默认不持久化及授权持久化；
- 一个核心假设允许必要连续动作，多个无关项目失败。

### 13.3 UX 门槛

核心 UX 继续使用十维 20 分 rubric，建议总分不低于 17/20，且真实目的对齐、终端可读性、可纠错性、问题可回答性和用户自主权均为 2。

增强能力使用八维 16 分 rubric，建议总分不低于 14/16，且成本与权限透明、综合责任、解决方案完整性均为 2，严重失败为 0。

没有真实 transcript、工具 trace 或用户评审时，不得评分或宣称通过。

### 13.4 发布支持范围与证据发布规则

v0.2.0 可以对外声明：当前正式协议版本、Claude Code 中显式 `/think-it-through` 入口、纯文本跨宿主基线、仓库机械校验事实，以及条件能力所遵守的路由、授权和回执合同。

以下信息必须分层维护：

- **发布支持范围**：写入 README、正式合同和版本记录，说明当前支持入口、纯文本基线与条件能力边界；
- **内部评测状态**：只在 `skills/think-it-through/evals/` 的机器文件与 rubric 中记录，不复制到公开文档或运行时分发包；
- **具体会话执行事实**：只由当次 capability observation、consent、工具 trace、receipt 和失败降级记录建立；
- **宿主原生兼容声明**：扩大范围前必须有对应版本的加载、能力、交互、授权、执行与降级证据。

正式发布不把 Adapter、schema、fixture、单元测试或线框转化为某次真实调用、原生 UI、Agent 启动、持久化或真实用户体验声明。冻结 v0.1 行为快照和 discovery holdout 只证明当时固定版本与场景，不证明 v0.2.0。
